name: Test Banner Script

on:
  workflow_dispatch:

jobs:
  test-banner:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run customize.sh
        run: |
          chmod +x customize.sh
          ./customize.sh
          echo "--- Banner content ---"
          cat files/etc/banner
