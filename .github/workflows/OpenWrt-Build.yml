name: Test Release OpenWrt x86_64

on:
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set test version
        id: set_version
        run: echo "fullversion=v24.10.2" >> $GITHUB_OUTPUT

      - name: Prepare dummy artifact
        run: |
          mkdir -p openwrt/bin/targets/x86/64
          echo "dummy openwrt firmware file" > openwrt/bin/targets/x86/64/openwrt-x86-64-generic-squashfs-combined-efi.img.gz

      - name: Create GitHub Release and upload dummy firmware
        uses: ncipollo/release-action@v1
        with:
          token: ${{ secrets.IGH_TOKEN }}
          tag: ${{ steps.set_version.outputs.fullversion }}
          name: OpenWrt x86_64 ${{ steps.set_version.outputs.fullversion }}
          body: |
            🎉 OpenWrt x86_64 ${{ steps.set_version.outputs.fullversion }}
            📅 构建时间：$(date '+%Y-%m-%d %H:%M:%S')
          artifacts: openwrt/bin/targets/x86/64/openwrt-x86-64-generic-squashfs-combined-efi.img.gz
          makeLatest: true
          skipIfReleaseExists: true
